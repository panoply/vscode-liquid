{%- liquid

  comment
  BLOCK COMMENT
  endcomment

  if foo == bar
    object.property['foo'][0]
  endif

  # INLINE COMMENT

  assign bar = '' | filter: 'string' | ceil: 10

  for i in x (1..10)

    capture name
    100.20
    endcapture

    section 'section'
    render 'snippet' with collection.products as product, param: object[product.id]

  endfor

-%}

